// Manually bumped on every commit — see the "Version bump" rule in
// CLAUDE.md. Never resets; only ever goes up. Lets the footer's version
// badge tell same-day deploys apart even though the date portion repeats.
export const BUILD_NUMBER = 11;
